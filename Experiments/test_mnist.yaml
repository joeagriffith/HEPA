cfgs:
  - dataset: mnist
    experiment: local_delme
    trial: iGPA
    model_type: iGPA
    save: True
    num_epochs: 2
    warmup: 0

  - dataset: mnist
    experiment: local_delme
    trial: iJEPA
    model_type: iJEPA
    save: True
    num_epochs: 2
    warmup: 0

  - dataset: mnist
    experiment: local_delme
    trial: AE
    model_type: AE
    save: True
    num_epochs: 2
    warmup: 0

  - dataset: mnist
    experiment: local_delme
    trial: BYOL
    model_type: BYOL
    save: True
    num_epochs: 2
    warmup: 0
